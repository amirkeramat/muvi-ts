import { NextResponse } from "next/server";
import { prismadb } from "@/libs/prismadb";

export async function GET(
    req:Request,
    {params}:{params:{
        mediaType:string
    }}
)
{

    try {
        
    } catch (error) {
        
    }

}